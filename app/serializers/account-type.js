import ApplicationSerializer from './application';

export default ApplicationSerializer.extend({
  // payloadKeyFromModelName(attr, method) {
  //   console.log(attr, method);
  //   return attr;
  // }
});
